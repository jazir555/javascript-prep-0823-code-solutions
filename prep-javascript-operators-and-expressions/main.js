const number1 = 1
const number2 = 2
const product = number1 * number2
console.log(product)
console.log("The type of product is", typeof product);
const charge = 5
const payment = 4
const amountRemaining = charge - payment
console.log(amountRemaining)
console.log("The type of amountRemaining is", typeof amountRemaining);
const tests = 3;
const assignments = 4;
const final = 5;
const grade = (assignments + tests + final) / 3;
console.log(grade);
console.log('The type of grade is', typeof grade);
const firstName = Bill
const lastName = Candy
const fullName = firstName + lastName
console.log(fullName);
console.log('The type of fullName is', typeof fullName);
const pH = 5
const isAcidic = pH < 7
console.log(isAcidic);
console.log('The type of isAcidic is', typeof isAcidic);
const numberOfSoldiers = 30
const isSparta = numberOfSoldiers === 300
console.log(isSparta);
console.log('The type of isSparta is', typeof isSparta);
console.log(isAcidic);
let nickname = fullName
Let nickname = fullName + ' is the GOAT'
console.log('The type of nickname is', typeof nickname);
